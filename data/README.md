# data/

Generated test JSON (synthetic, with hidden ground truth) produced by `generator/`.

- **Synthetic only** for this spike. No real game logs this round (Stage B deferred).
- Files are reproducible from config + seed, so large outputs may be gitignored and regenerated on demand.
- `games` is **Level-0 only**; everything above (Levels 1–3) is derived by the harness, never stored as input.
